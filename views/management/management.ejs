<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <link rel="shortcut icon" href="https://www.bravecollective.com/i/logo.png" type="image/x-icon">
        <link
            rel="stylesheet"
            href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css"
        />
        <style>
            .brave-primary {
                background-color: #007ba0;
            }
            a:not(.normal-sized) {
                color: #333333;
                font-size: 1.5em;
                padding-right: 20px;
            }
            .large-text {
                font-size: 1.5em;
            }
            .icon {
                padding-right: 10px;
                padding-left: 10px;
            }

            .dropdown-button {
                cursor: pointer;
            }

            .dropdown {
                position: relative;
                display: inline-block;
            }

            .dropdown-content {
                display: none;
                position: absolute;
                right: 0;
                top: 50px;
                background-color: #f1f1f1;
                min-width: 200px;
                box-shadow: 0px 8px 16px 0px rgba(0,0,0,0.2);
                z-index: 1;
            }

            .dropdown-content a {
                color: black;
                padding: 12px 16px;
                text-decoration: none;
                display: block;
            }

            .dropdown-content a:hover {
                background-color: #ddd;
            }

            .show {
                display: block;
            }
        </style>
        <script>
            function toggleDropdown() {
                document.getElementById("dropdown-id").classList.toggle("show");
            }
        </script>
        <title>BraveBucks - Management</title>
    </head>
    <body>
        <nav class="navbar navbar-expand-lg brave-primary">
            <a class="navbar-brand icon" href="#">
                <img
                    src="https://www.bravecollective.com/i/logo.png"
                    width="52"
                    height="45"
                    alt=""
                />
            </a>
            <a class="navbar-brand large-text" href="/">BraveBucks</a>
            <a href="/personal">Personal</a>
            <% if (user) { %>
                <% if (user.role === "manager" || user.role === "admin") { %>
                    <a href="/management">Management</a>
                <% } %>
             <div class="ml-auto">
                <div class="dropdown">
                    <img src="https://images.evetech.net/characters/<%= user.user_id %>/portrait?tenant=tranquility&size=64" height="45px" width="auto" class="dropdown-button" alt="Profile" onclick="toggleDropdown()" >
                    <div id="dropdown-id" class="dropdown-content">
                        <a class="normal-sized" href="/personal"><%= user.username %></a>
                        <a class="normal-sized" href="/">Home</a>
                        <a class="normal-sized" href="/auth/logout">Log Out</a>
                    </div>
                </div>
            </div>
            <% } else { %>
            <a class="navbar-brand ml-auto" href="/auth/oauth2">
                <img
                    src="https://web.ccpgamescdn.com/eveonlineassets/developers/eve-sso-login-black-large.png"
                    alt=""
                />
            </a>
            <% } %>
        </nav>
        <h1 class="text-center">Management Center</h1>
        <br>
            <div class="mx-auto col-md-3"></div>
            <div class="mx-auto col-md-6">
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title
                        ">Users</h5>
                        <p class="card-text">View details of the registered users in the database and manage them. Currently, there are <%= statistics.users %> users registered in the database.</p>
                        <a href="/management/users" class="normal-sized btn btn-primary">View</a>
                    </div>
                </div>
            </div>
            <div class="mx-auto col-md-3"></div>
        <br>
            <div class="mx-auto col-md-3"></div>
            <div class="mx-auto col-md-6">
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title
                        ">Killmails</h5>
                        <p class="card-text">
                            Currently, there are <%= statistics.unaccounted %> killmails that are not accounted for. Killmails are accounted for every 24 hours. 
                            When this cycle activates, all killmails that are unaccounted for will be processed and the ISK (<%= parseFloat(killPool ?? 0).toLocaleString("en") %>) will be distributed to the users.
                            This is done based on the amount of participants and the points they have earned. Additionaly, every user will receive a flat amount of ISK (<%= parseFloat(killFlat ?? 0).toLocaleString("en") %>) for participating.
                        </p>
                    </div>
                </div>
            </div>
            <div class="mx-auto col-md-3"></div>
        <br>
            <div class="mx-auto col-md-3"></div>
            <div class="mx-auto col-md-6">
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title
                        ">Payout Requests</h5>
                        <p class="card-text">View and manage payout requests. Users can request payouts when reaching a certain threshold of ISK (<%= parseFloat(amountRequired ?? 0).toLocaleString("en") %>). Currently, there are <%= statistics.payouts.length ?? 0 %> pending payout requests.</p>
                        <a href="/management/requests" class="normal-sized btn btn-primary">View</a>
                    </div>
                </div>
            </div>
            <div class="mx-auto col-md-3"></div>
        <br>
            <div class="mx-auto col-md-3"></div>
            <div class="mx-auto col-md-6">
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title
                        ">Settings</h5>
                        <p class="card-text">Access and change the configurations. Needs the admin role.</p>
                        <a href="/management/settings" class="normal-sized btn btn-primary">View</a>
                    </div>
                </div>
            </div>
            <div class="mx-auto col-md-3"></div>
    </body>
</html>