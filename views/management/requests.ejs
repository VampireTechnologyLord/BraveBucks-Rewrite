<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <link rel="shortcut icon" href="https://www.bravecollective.com/i/logo.png" type="image/x-icon">
        <link
            rel="stylesheet"
            href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css"
        />
        <style>
            .brave-primary {
                background-color: #007ba0;
            }
            a:not(.normal-sized) {
                color: #333333;
                font-size: 1.5em;
                padding-right: 20px;
            }
            .large-text {
                font-size: 1.5em;
            }
            .icon {
                padding-right: 10px;
                padding-left: 10px;
            }

            .dropdown-button {
                cursor: pointer;
            }

            .dropdown {
                position: relative;
                display: inline-block;
            }

            .slider {
                -webkit-appearance: none;
                width: 100%;
                height: 25px;
                background: #d3d3d3;
                outline: none;
                opacity: 0.7;
                -webkit-transition: .2s;
                transition: opacity .2s;
            }

            .dropdown-content {
                display: none;
                position: absolute;
                right: 0;
                top: 50px;
                background-color: #f1f1f1;
                min-width: 200px;
                box-shadow: 0px 8px 16px 0px rgba(0,0,0,0.2);
                z-index: 1;
            }

            .dropdown-content a {
                color: black;
                padding: 12px 16px;
                text-decoration: none;
                display: block;
            }

            .dropdown-content a:hover {
                background-color: #ddd;
            }

            .show {
                display: block;
            }
        </style>
        <script>
            function toggleDropdown() {
                document.getElementById("dropdown-id").classList.toggle("show");
            }
        </script>
        <title>BraveBucks - Requests</title>
    </head>
    <body>
        <nav class="navbar navbar-expand-lg brave-primary">
            <a class="navbar-brand icon" href="#">
                <img
                    src="https://www.bravecollective.com/i/logo.png"
                    width="52"
                    height="45"
                    alt=""
                />
            </a>
            <a class="navbar-brand large-text" href="/">BraveBucks</a>
            <a href="/personal">Personal</a>
            <% if (user) { %>
                <% if (user.role === "manager" || user.role === "admin") { %>
                    <a href="/management">Management</a>
                <% } %>
             <div class="ml-auto">
                <div class="dropdown">
                    <img src="https://images.evetech.net/characters/<%= user.user_id %>/portrait?tenant=tranquility&size=64" height="45px" width="auto" class="dropdown-button" alt="Profile" onclick="toggleDropdown()" >
                    <div id="dropdown-id" class="dropdown-content">
                        <a class="normal-sized" href="/personal"><%= user.username %></a>
                        <a class="normal-sized" href="/">Home</a>
                        <a class="normal-sized" href="/auth/logout">Log Out</a>
                    </div>
                </div>
            </div>
            <% } else { %>
            <a class="navbar-brand ml-auto" href="/auth/oauth2">
                <img
                    src="https://web.ccpgamescdn.com/eveonlineassets/developers/eve-sso-login-black-large.png"
                    alt=""
                />
            </a>
            <% } %>
        </nav>
        <br>
        <h2 class="text-center">Pending Payout Requests</h2>
        <br>
        <div class="container">
            <table class="table table-striped">
                <tr>
                    <th scope="col">Character</th>
                    <th scope="col">Amount</th>
                    <th scope="col">Status</th>
                    <th scope="col">Date</th>
                    <th scope="col">Action</th>
                </tr>
                <% for (let request of requests) { %>
                    <% if (request.status == "pending") { %>
                    <tr>
                        <td><%= request.username %></td>
                        <td><%= parseFloat(request.amount).toLocaleString("en") %></td>
                        <td><%= request.status %></td>
                        <td><%= `${new Date(request.date).getFullYear()}-${new Date(request.date).getMonth() + 1}-${new Date(request.date).getDate()} ${new Date(request.date).getHours()}:${new Date(request.date).getMinutes()}:${new Date(request.date).getSeconds()}` %></td>
                        <td>
                            <a class="normal-sized btn btn-success" href="/management/requests/approve?id=<%= request.id %>">Approve</a>
                            <a class="normal-sized btn btn-danger" href="/management/requests/deny?id=<%= request.id %>">Deny</a>
                        </td>
                    </tr>
                    <% } %>
                <% } %>
            </table>
        </div>
        <br>
        <h2 class="text-center">Managed Payout Requests</h2>
        <br>
        <div class="container">
            <table class="table table-striped">
                <tr>
                    <th scope="col">Character</th>
                    <th scope="col">Amount</th>
                    <th scope="col">Status</th>
                    <th scope="col">Date</th>
                </tr>
                <% for (let request of requests) { %>
                    <% if (request.status != "pending") { %>
                    <tr>
                        <td><%= request.username %></td>
                        <td><%= parseFloat(request.amount).toLocaleString("en") %></td>
                        <td><%= request.status %></td>
                        <td><%= `${new Date(request.date).getFullYear()}-${new Date(request.date).getMonth() + 1}-${new Date(request.date).getDate()} ${new Date(request.date).getHours()}:${new Date(request.date).getMinutes()}:${new Date(request.date).getSeconds()}` %></td>
                    </tr>
                    <% } %>
                <% } %>
            </table>
        </div>
    </body>
</html>